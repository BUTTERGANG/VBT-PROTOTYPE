{
  "name": "vbt-autoregulate",
  "version": "1.0.0",
  "description": "VBT autoregulation service — velocity-based training recommendations",
  "main": "main.py",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
  },
  "dependencies": {
    "fastapi": "^0.115.0",
    "uvicorn": "^0.34.0",
    "pydantic": "^2.10.0",
    "httpx": "^0.28.0"
  }
}
