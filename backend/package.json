{
  "name": "vbt-tracker-backend",
  "version": "0.1.0",
  "description": "VBT Tracker REST API",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:init": "node -e \"const {neon} = require('@neondatabase/serverless'); const fs = require('fs'); const url = process.env.DATABASE_URL; if(!url){console.error('DATABASE_URL not set');process.exit(1)} const sql = neon(url + (url.includes('sslmode=') ? '' : (url.includes('?') ? '&' : '?') + 'sslmode=require')); const ddl = fs.readFileSync('src/db/schema.sql','utf8'); (async()=>{for(const s of ddl.split(';').filter(x=>x.trim())){await sql(s + ';')} console.log('DB initialized')})().catch(e=>{console.error(e);process.exit(1)})\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.9",
    "@types/uuid": "^11.0.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
